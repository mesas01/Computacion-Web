spring.application.name=entrega
gg.jte.development-mode=true

# Configuracion H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# Configuracion de base de datos postgresql
#spring.datasource.url=jdbc:postgresql://192.168.0.19:5432/entrega_db
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.username=sergio
#spring.datasource.password=123

# Configuracion JPA/Hibernate
#le dice a Hibernate que use dialecto PostgreSQL.
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#actualiza la estructura de tablas automáticamente según tus entidades.
spring.jpa.hibernate.ddl-auto=update
#imprime en consola las queries que se ejecutan.
spring.jpa.show-sql=true
#deja que scripts SQL se ejecuten después de montar el datastsource
spring.jpa.defer-datasource-initialization=true

# Inicializacion SQL
spring.sql.init.mode=always

#Logging
#permite imprimir la query
logging.level.org.hibernate.SQL=DEBUG
#imprime los parametros de las querys
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuracion de base de datos postgresql
spring.datasource.url=jdbc:postgresql://localhost:5432/entrega_db
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=sergio
spring.datasource.password=123
